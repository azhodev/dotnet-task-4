@{
    ViewData["Title"] = "Blocked user";
}

<section class="status-page mx-auto" style="max-width: 720px;">
    <div class="alert alert-danger shadow-sm" role="alert">
        Your account is blocked or no longer exists. Please log in again with another account.
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-4">
            <h1 class="h4">Сценарий из задания</h1>
            <p class="mb-0">Любой запрос (кроме login/register) от удалённого или заблокированного пользователя должен приводить к переадресации на страницу входа.</p>
        </div>
    </div>

    <div class="d-flex flex-wrap gap-2 mt-3">
        <a class="btn btn-primary" asp-controller="Mockups" asp-action="Login">Back to login</a>
        <a class="btn btn-outline-secondary" asp-controller="Mockups" asp-action="UserManagement">Preview user table</a>
    </div>
</section>
